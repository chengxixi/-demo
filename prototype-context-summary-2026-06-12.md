# 原型修改上下文总结（2026-06-12）

## 当前工作范围

项目目录：`/Users/handy/codex/produce_iteration_system`

当前主要修改对象：

- `feedback-management-prototype/src/App.jsx`
- `feedback-management-prototype/src/styles.css`

当前聚焦模块：

- 反馈清单
- 需求管理
- 竞争分析 / 竞品信息
- 首页仪表盘
- 侧边栏交互

用户已明确说明：GitHub 推送由用户手动操作，后续不要自动推送。

## 已完成的主要改动

### 反馈清单

- 主表支持行内快速选择：
  - 一级分类
  - 二级分类
  - 三级分类
  - 情绪
  - 是否退货
  - 异常等级建议
- 去掉主表 `责任人` 列。
- `状态` 改为只读，并进一步改名为 `处理去向`。
- 反馈详情底部动作改成明确去向：
  - `生成工单`：进入工单池。
  - `转异常`：进入异常处理。
  - `生成候选需求`：进入需求管理的候选线索。
  - `加入Q&A`：进入 Q&A/案例库。
- 反馈详情动作会写回主表 `处理去向`。
- 顶部汇总卡去掉没有对比来源的变化百分比，只保留当前指标值。
- 工具栏 `更多操作` 改为 `同步`。
- 工具栏末尾增加同步状态：
  - 同步时间
  - 新增数量
- 第一条反馈增加绿色 `新增` 标识点。
- 鼠标悬浮绿色点时显示提示：`新增`。

### 需求管理

- 顶部 L1-L4 卡片支持点击筛选。
- 再次点击同一个等级卡片时恢复全部。
- 新增 `候选线索 / 产品需求池` 页签。
- `生成候选需求` 不再直接进入正式需求池，而是先生成候选需求线索。
- 候选线索表字段包括：
  - 线索ID
  - 来源反馈
  - 分类
  - 线索标题
  - 证据摘要
  - 适用产品
  - 状态
  - 操作
- 候选线索操作压缩为一行：
  - 合并
  - 补证据
  - 进入评分

### 首页

- 保留首页筛选栏。
- `关键指标趋势（近90天）` 改为跟随顶部时间周期选择变化。
  - 例如：`近30天` 对应 `关键指标趋势（近30天）`。

### 竞争分析 / 竞品信息

- 菜单 `竞品监控` 已改为 `竞争分析`。
- 竞争分析页已拆为三个页签：
  - 市场监控
  - 舆情监测
  - 竞品信息
- 竞品信息详情面板可关闭。
- 产品对比改为独立弹层 / 右侧页面。
- 对比选择区已从产品卡片列表改为 4 个固定下拉选择槽位。
- 正在扩展产品对比表，让对比表包含完整参数字段。

### 侧边栏

- 去掉侧边栏底部系统状态和同步状态文字。
- 保留收起菜单图标。
- 收起菜单已实现真实折叠效果：
  - 折叠后侧边栏变窄。
  - 只显示品牌图标和菜单图标。
  - 主内容区同步左移并扩大。
  - 再次点击图标可展开。

## 当前需优先检查的问题

最近一次修改正在处理“产品对比也要把所有参数都增加上去”，但修改过程中被中断。

下轮继续时必须优先检查：

1. `ProductCompareDrawer` 是否有语法或 import 问题。
2. 当前 `App.jsx` 里新增了 `React.Fragment` 结构，但文件顶部目前可能没有默认导入 `React`。
3. 推荐处理方式：
   - 使用 `Fragment`：
     - `import { Fragment, useMemo, useState } from "react";`
     - 将 `<React.Fragment key={group}>` 改为 `<Fragment key={group}>`
   - 或改为默认导入 React。
4. 先运行：
   - `git status --short`
   - `npm run build`
5. 如果 build 失败，优先修复 `ProductCompareDrawer` 相关代码。

## 建议下轮继续顺序

1. 运行 `git status --short` 查看当前未提交文件。
2. 运行 `npm run build`。
3. 修复产品对比全量参数表可能存在的构建错误。
4. 完成产品对比表字段：
   - 基础档案
   - 价格与评价
   - 硬件规格
   - 测量与算法 / 功能能力
   - 连接与数据
   - 包装售后与证据
   - 核心卖点
   - 用户痛点
5. 检查对比弹层的横向滚动和阅读性。
6. 再继续处理新的浏览器评论。

## 当前验证状态

- 多次 `npm run build` 已通过。
- 最后一次“产品对比全量参数”修改被中断后，尚未重新运行 build。
- 下轮必须先重新构建验证。

## 注意事项

- 不要读取或复用旧线程的大截图 base64 内容。
- 后续输出默认中文。
- 优先形成可试运行 MVP。
- 区分流程文件、标准文件、表单模板和 IT 系统设计。
- 不要自动推送 GitHub，除非用户重新明确要求。
